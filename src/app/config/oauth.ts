// Deprecated: OAuth config lives in index.ts now. Kept to avoid import breakage.
export { default as config } from './index';

// Default export matches the original named export for compatibility.
export { default } from './index';
